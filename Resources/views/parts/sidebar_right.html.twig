{% for item in items %}
    {{ render(
        controller(
            "ez_content:viewLocation",
            {
                "locationId": item.id,
                "viewType": "line"
            }
        )
    ) }}
{% endfor %}