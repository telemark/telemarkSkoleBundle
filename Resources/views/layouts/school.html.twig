<div data-studio-zones-container>
    {#{ dump( zones ) }#}
    <div class="landing-page__zone landing-page__zone--{{ zones[0].id }}" data-studio-zone="{{ zones[0].id }}">
        {% if zones[0].blocks %}
            {% for block in zones[0].blocks %}
                <div class="landing-page__block block_{{ block.type }}">
                    {{ render_esi(controller('ez_block:renderBlockAction', {
                        'contentId': contentInfo.id,
                        'blockId': block.id
                    })) }}
                </div>
            {% endfor %}
        {% endif %}
    </div>
    <div class="landing-page__zone landing-page__zone--{{ zones[1].id }}" data-studio-zone="{{ zones[1].id }}">
        {% if zones[1].blocks %}
            {% for block in zones[1].blocks %}
                <div class="landing-page__block block_{{ block.type }}">
                    {{ render_esi(controller('ez_block:renderBlockAction', {
                        'contentId': contentInfo.id,
                        'blockId': block.id
                    })) }}
                </div>
            {% endfor %}
        {% endif %}
    </div>
    <div class="landing-page__zone landing-page__zone--{{ zones[2].id }}" data-studio-zone="{{ zones[2].id }}">
        {% if zones[2].blocks %}
            {% for block in zones[2].blocks %}
                <div class="landing-page__block block_{{ block.type }}">
                    {{ render_esi(controller('ez_block:renderBlockAction', {
                        'contentId': contentInfo.id,
                        'blockId': block.id
                    })) }}
                </div>
            {% endfor %}
        {% endif %}
    </div>
</div>
