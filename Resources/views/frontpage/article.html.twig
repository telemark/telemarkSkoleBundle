<div class="item item--vertical grid__item--6-12 landing-article">
  <div>
{% if not ez_is_field_empty( content, 'image' ) %}
        {{ ez_render_field( content, 'image', {
            'parameters': {
                'alias': 'article_grid_2',
                'class': 'img-responsive'
            }
        }  ) }}
    {% else %}
          <figure class="ezimage-field">
            <img src="{{ asset('bundles/tfktelemarkskole/images/contenttype/bildetmangler.jpg') }}" alt="Mappe" />
        </figure>
    {% endif %}
        <div class="intro-text">
            <h1><a href="{{ path(location) }}">
                {{ ez_render_field( content, 'title' ) }}
            </a>
        </h1>
            {{ ez_render_field( content, 'intro' ) }}
        </div>
     </div>
 </div>