<div class="item item--vertical grid__item--4-12">
    <div>
        {#% if not ez_is_field_empty( content, 'image' )%}
            {{ ez_render_field(
                content, 'image', {
                    'parameters': {
                        'alias': 'article_grid_3',
                        'class': 'img-responsive'
                    }
                }
            ) }}
        {% endif %#}
        <h2><a href="{{ path(location) }}">{{ ez_render_field( content, 'title') }}</a></h2>
    </div>
</div>
